# Copy to .env.local. This file is consumed only by Docker Compose.
# Set Y only after reviewing and accepting both license terms:
# https://github.com/Azure/azure-service-bus-emulator-installer/blob/main/EMULATOR_EULA.txt
# https://go.microsoft.com/fwlink/?LinkId=746388
ACCEPT_EULA=N

# Required only for repository-root full-stack mode:
# docker compose --env-file Layer1_Data_foundations/.env.local --profile full-stack up
# Use a local-only password that satisfies SQL Server password policy.
MSSQL_SA_PASSWORD=<generate-a-strong-local-password>
