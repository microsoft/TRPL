# Multi-camera room monitoring system dependencies

# Core dependencies
opencv-python==4.11.0.86
numpy==1.26.4
ultralytics==8.4.123  # YOLOv8

# Intel RealSense SDK (optional — only needed if using realsense:// camera source)
# Prebuilt wheels: Linux x86_64, Windows, macOS x86_64. Apple Silicon needs source build.
pyrealsense2==2.56.5.9235

# Cross-camera person re-identification
torchreid==0.2.5

# LLM integration (optional, auto-degrades without API Key)
openai==3.0.0

# API service
flask==3.1.3
flask-cors==6.0.5

# HTTP client for event forwarding to agent server (transport="http")
requests==2.34.2

# WebSocket server for event delivery (transport="ws", the default) — camera
# hosts a WS endpoint that downstream connects to and drains.
websockets==17.0.1

# .env file loading
python-dotenv==1.2.2

# Development tools (optional)
pytest==8.4.2
